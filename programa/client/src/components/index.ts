/**
 * Barrel export for components
 * Simplifies imports: import { Loading, ScoreBoard } from '@/components'
 */

export { Loading } from './Loading';
export { ScoreBoard } from './ScoreBoard';
export { TableroGrid } from './TableroGrid';
