# Configuración del Cliente Match-3
# ===================================
# El cliente detecta automáticamente si localhost o ngrok están disponibles.
#
# DETECCIÓN AUTOMÁTICA:
# 1. Primero intenta conectar a localhost:4000
# 2. Si localhost no responde y REACT_APP_NGROK_URL está configurado, usa ngrok
# 3. Si el cliente se accede via ngrok, automáticamente usa el backend de ngrok
#
# SOLO LOCALHOST (desarrollo normal):
# Deja REACT_APP_NGROK_URL vacío o no lo definas
#
# CON NGROK (para compartir con otros):
# 1. Ejecuta: cd server && npm run ngrok
# 2. Copia la URL del backend que te muestra
# 3. Pégala aquí:

REACT_APP_NGROK_URL=
